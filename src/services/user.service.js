
const model = require("../model")
const utility = require("../utility")
const customer = require("./customer.service");

const user = {

    create: async (req) => {

    },
    read: async (req) => {
    },

    update: async (req) => {
    },

    delete: async (req) => {
    }
}

module.exports = user

